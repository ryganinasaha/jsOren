
function checkPosverd(posverd){
    if ( posverd === `Step`) {
        return (`true`)
    } if ( posverd === `Web`) {
        return (`true`)
    } if ( posverd === `JavaScript`) {
        return (`true`)
    } else {
        return (`fols`)
    }
}
checkPosverd(``)


/*function checkPosverd() {
   return (x === `Step`) || ( x === `Web` ) ||(x === `JavaScript`) 
    }

    console.log()*/
    